version: 0.0
os: linux

files:
  - source: pipeline-0.0.1-SNAPSHOT.jar
    destination: /opt/pipeline

hooks:
  AfterInstall:
    - location: scripts/restart.sh
      timeout: 300
      runas: ubuntu
